---
description: 修改代码后自动同步到 Git 远程仓库
alwaysApply: true
---

# Git 自动同步

当用户请求修改代码并明确要求「自动同步」「同步到 git」「推送到远程」等时，在完成所有代码修改后，执行以下步骤：

1. **检查状态**：使用 `git_status` 确认有未提交的更改
2. **暂存更改**：使用 `git_add_or_commit`，action 为 `add`，暂存所有修改
3. **提交**：使用 `git_add_or_commit`，action 为 `commit`，提交信息应简洁描述本次修改内容
4. **推送**：使用 `git_push` 推送到远程仓库

## 使用方式

在请求修改代码时，加上类似表述即可：
- 「修改 xxx，完成后自动同步到 git」
- 「帮我改一下 xxx，改完自动推送」
- 「修改并同步到远程仓库」

## 注意事项

- 仅在用户**明确要求**自动同步时才执行，避免未经同意的推送
- 提交信息应清晰描述修改内容，便于版本追溯
